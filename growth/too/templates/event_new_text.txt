The GROWTH Target of Opportunity Marshal received a new {{ event.tags|join(' ') }} event about {{ (now() - event.dateobs)|humanize() }} at {{ event.dateobs.isoformat() }}: {{ url_for('event', dateobs=event.dateobs, _external=True) }}
